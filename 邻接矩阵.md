# 邻接矩阵

用于表示图的数据结构

- 一般用在无向图中，在无向图中比邻接表方便

**结构定义**

- 矩阵中的值表示两个顶点之间的连接情况（可以记录权值）

  ```C
  // 结点结构
  typedef struct {
  	int id;		// 编号
  	char* show;	// 数据存储
  } MatrixVertex;
  
  // 邻接矩阵的边结构，每条边的结构，用int空间表示，既可以描述权值，也可以描述布尔值是否有边
  typedef int MaxtrixEdge;
  
  #define MAX_NODE_NUM 20
  #define INF 10000
  
  // 邻接矩阵图结构，核心：顶点集，邻接矩阵（边的信息），图头
  typedef struct {
  	MatrixVertex vex[MAX_NODE_NUM];	// 顶点信息
  	int nodeNum;					// 顶点个数
  	MaxtrixEdge edges[MAX_NODE_NUM][MAX_NODE_NUM];	// 邻接矩阵，边的信息（顶点之间的关系）
  	int direction;	// 是否为有向图，0无向，1有向
  	int edgeNum;	// 边的个数
  } MGraph;
  ```

- 如何使用

  1. 初始化：顶点信息，边信息，图头信息
  2. 添加顶点(修改关系)：直接对邻接矩阵下标对应的值进行修改

### 邻接矩阵的遍历

两种方式：深搜（DFS）和广搜（BFS）

1. **图的DFS**

   从某个结点开始，找到一个边结点，再通过结点找下一个，DFS
   注意：记录已经遍历过的结点，防止在别的结点DFS到这里后再次访问

   > 这里用桶记录是否访问过

   ```C
   void DFSMGraphTravel(const MGraph* graph, int startV) {
   	visitMGraphNode(&graph->vex[startV]);
   	MGraphVisited[startV] = 1;
   	for (int i = 0; i < graph->nodeNum; ++i) {
   		// 从startV开始，找到一个边结点，再通过这个结点找下一个结点，DFS
   		// 遍历邻接矩阵
   		if (graph->edges[startV][i] && MGraphVisited[i] == 0) {
   			DFSMGraphTravel(graph, i);
   		}
   	}
   }
   ```

2. **图的BFS**
   用队列进行缓存
   存入缓存区的时候记录，防止在别的结点BFS到这里后再次访问

   ```C
   void BFSMGraphTravel(const MGraph* graph, int startV) {
   	int que[MAX_NODE_NUM];
   	int rear = 0, front = 0;
   
   	initMGraphVisit(MGraphVisited);
   	// 初始化缓存区
   	rear = (rear + 1) % MAX_NODE_NUM;
   	que[rear] = startV;
   	MGraphVisited[startV] = 1;
   
   	// BFS
   	while (front != rear) {
   		// 做缓存中的任务
   		front = (front + 1) % MAX_NODE_NUM;
   		int cur = que[front];
   		visitMGraphNode(&graph->vex[startV]);
   
   		// 缓存该结点对应邻接矩阵那一层的未访问过的结点
   		for (int i = 0; i < MAX_NODE_NUM; ++i) {
   			if (graph->edges[cur][i] && MGraphVisited[i] == 0) {
   				rear = (rear + 1) % MAX_NODE_NUM;
   				que[rear] = i;
   				MGraphVisited[i] = 1;
   			}
   		}
   	}
   }
   ```

   



