# 高阶搜索树

## 1. 红黑树

- 红黑树的应用举例:IO多路复用的**epoll**(poll的优化),由线性(n)到logn,提高性能和效率

- 需要了解/属性插入和删除操作

- 需要熟悉相关性质（**黑高树**）

  (1) 每个节点或者是⿊⾊，或者是红⾊。
  (2) 根节点是⿊⾊。
  (3) 每个叶⼦节点是⿊⾊。 [注意：这⾥叶⼦节点，是指为空的叶⼦节点！]
  (4) 如果⼀个节点是红⾊的，则它的⼦节点必须是⿊⾊的。[不存在两个相邻的红⾊节点]
  (5) 从⼀个节点到该节点的⼦孙节点的所有路径上包含相同数⽬的⿊节点。[⿊⾼相同]

- 特点(和平衡树比较)：查找和插入两者的效率平衡了(在工程上：平衡树查找极快，插入效率低)

## 2. B树

- 应用：目前应用较少(一些古老的应用可能会用)，更多的被B+树取代

- 性质：

  1）每个结点最多有m-1个关键字；
  2）根节点最少可以只有1个关键字；
  3）⾮根节点⾄少有ceil(m/2) - 1个关键字；
  4）每个节点中的关键字都按照从⼩到⼤的顺序排列，每个关键字的左⼦树的所有关键字都⼩于它，
  ⽽右⼦树中的所有关键字都⼤于它；
  5）所有叶⼦节点都位于同⼀层，或者说根节点到每个叶⼦节点的⻓度相同；

- 需要了解它的插入和删除操作

- 特点(相较于B+树)：其真实数据分布在树上的每个结点的元素，**每个元素既可以当索引又可以当数据**

### 3. B+树

- 应用举例：很多数据库，便于管理，效率稳定(数据全再叶子节点上)，磁盘IO操作相较于B树更少(树更矮)

- 性质：

  1）每个结点最多有m-1个关键字；
  2）根节点最少可以只有1个关键字；
  3）⾮根节点⾄少有ceil(m/2) - 1个关键字；
  4）每个节点中的关键字都按照从⼩到⼤的顺序排列，每个关键字的左⼦树的所有关键字都⼩于它，
  ⽽右⼦树中的所有关键字都⼤于它；
  5）所有叶⼦节点都位于同⼀层，或者说根节点到每个叶⼦节点的⻓度相同；

- 特点(相较于B树)：**真实数据都在叶子结点，索引和数据分离（所有非叶子结点结点的元素都用于索引(大小)），数据极值继承，可以进行更高效的区间查找**

