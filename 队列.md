# 队列

逻辑结构：1:1,表结构

普遍(链表,栈,队列等)： 插入/删除行为上增加约束



## 顺序队列

> 循环顺序队列

特点:

1. 假溢出 (++尾指针)

2. 空队和满队 逻辑表达冲突

   - 增加一个标志解决冲突

   - 牺牲一个空间解决冲突（推荐）

     > 空队时两指针指向一致，满队时差1

- 代码示例：

  ```C
  #include <stdio.h>
  #include <stdlib.h>
  
  #include "ArrayQueue.h"
  
  //typedef int element_t;
  //
  ///* 定义顺序队列 表头 */
  //typedef struct {
  //	element_t data[MAX_CAPACITY];
  //	int front;
  //	int rear;
  //}arrayQueue;
  
  /* 初始化 */
  void initArrayQueue(arrayQueue* queue) {
  	queue->front = 0;
  	queue->rear = 0;
  }
  
  /* 入队 */
  void enArrayQueue(arrayQueue* queue, element_t val) {
  	if (isFullQueue(queue)) {
  		printf("queue FUll!\n");
  		return;
  	}
  	queue->data[queue->rear] = val;
  	queue->rear = (queue->rear + 1) % MAX_CAPACITY;
  }
  
  /* 出队 */
  void deArrayQueue(arrayQueue* queue) {
  	if (isEmptyQueue(queue)) {
  		printf("queue NONE!\n");
  		return;
  	}
  	queue->front = (queue->front + 1) % MAX_CAPACITY;
  }
  
  /* 清空 */
  void clearArrarQueue(arrayQueue* queue) {
  	queue->front = queue->rear = 0;
  }
  
  /* 判断是否满队 */
  int isFullQueue(arrayQueue* queue) {
  	return (queue->rear + 1) % MAX_CAPACITY == queue->front;
  }
  
  /* 是否空队 */
  int isEmptyQueue(arrayQueue* queue) {
  	return queue->rear == queue->front;
  }
  
  /* 获取头的值 */
  element_t getHeadArrayQueue(arrayQueue* queue) {
  	if (isEmptyQueue(queue)) {
  		printf("queue NONE!\n");
  		return -1;
  	}
  	return queue->data[queue->front];
  }
  ```

  

## 链式队列

没有满队的概念

- 代码示例：

  ```C
  #include <stdio.h>
  #include <stdlib.h>
  #include "LinkQueue.h"
  
  typedef int value;
  
  /* 定义 结点 */
  //typedef struct node {
  //	value data;
  //	struct node *next;
  //}QNode;
  
  /* 定义 队列表头 */
  //typedef struct {
  //	QNode* front;
  //	QNode* rear;
  //	int count;
  //}LinkQueue;
  
  /* 创建 链式队列 */
  LinkQueue* createLinkQueue() {
  	LinkQueue* q = (LinkQueue*)malloc(sizeof(LinkQueue));
  	if (q == NULL) {
  		return NULL;
  	}
  	q->count = 0;
  	q->front = NULL;
  	q->rear = NULL;
  	return q;
  }
  
  /* 入队 */
  int enLinkQueue(LinkQueue* queue, value val) {
  	QNode* node = (QNode*)malloc(sizeof(QNode));
  	if (node == NULL) {
  		return -1;
  	}
  	node->next = NULL;
  	node->data = val;
  	
  	if (queue->rear == NULL) {
  		queue->front = node;
  		queue->rear = node;
  	}
  	else {
  		queue->rear->next = node;
  		queue->rear = node;
  	}
  	queue->count++;
  	return 1;
  }
  
  /* 出队 */
  void deLinkQueue(LinkQueue* queue) {
  	QNode* temp;
  	if (queue->front == NULL) {
  		printf("the num of queue: %d\n", queue->count);
  	}
  	else {
  		temp = queue->front;
  		queue->front = queue->front->next;
  		if (queue->front == NULL) {
  			queue->rear = NULL;
  		}
  		free(temp);
  		--queue->count;
  	}
  }
  
  /* 释放 */
  void releaseLinkQueue(LinkQueue* queue) {
  	QNode* temp;
  	while (queue->front != NULL) {
  		temp = queue->front;
  		queue->front = queue->front->next;
  		free(temp);
  		--queue->count;
  	}
  	printf("the num of queue: %d\n", queue->count);
  	free(queue);
  }
  ```



## 补充

逻辑结构 物理结构 中对象的属性，逻辑结构相似时，其物理结构可能也相似

> 如 队列的front,rear的一致性
>
> 如 栈存和取的方向相反，操作也应该相反，队列方向相同）

链 ：

- 结点插入：先引入新结点，后 整合/调整 老结点
- 没有长度
- 删除要备份

顺序：

- 有长度