# 最短路径

**Dijkstra算法**

同样的，用邻接矩阵存储图的信息

重要的结构：

- **dist[]**:存储**起始点到该下标的最短距离**（下标是对应的点）
- **path[]**:下标表示该位置，值表示最短路径中**该点的上一个位置**（静态链表）
- **mark[]**:下标表示位置，值表示**激活状态**
  如果被激活，那么这个点所对应的最短路径已经被确定了

这是初始状态(**INF表示无穷大，-1表示没有上一个位置，0表示未被激活**)

|  ID  | dist(起始点到该点的距离) | path(上一个位置) | mark(顶点激活状态) |
| :--: | :----------------------: | :--------------: | :----------------: |
|  0   |           INF            |        -1        |         0          |
|  1   |           INF            |        -1        |         0          |
|  2   |           INF            |        -1        |         0          |
|  3   |           INF            |        -1        |         0          |
|  4   |           INF            |        -1        |         0          |
|  5   |           INF            |        -1        |         0          |
|  6   |           INF            |        -1        |         0          |

**邻接矩阵和结点定义：**

```C
// 结点结构
typedef struct {
	int id;		// 编号
	char* show;	// 数据存储
} MatrixVertex;

// 邻接矩阵的边结构，每条边的结构，用int空间表示，既可以描述权值，也可以描述布尔值是否有边
typedef int MaxtrixEdge;

#define MAX_NODE_NUM 20
#define INF 10000


// 邻接矩阵图结构
typedef struct {
	MatrixVertex vex[MAX_NODE_NUM];	// 顶点信息
	int nodeNum;					// 顶点个数
	MaxtrixEdge edges[MAX_NODE_NUM][MAX_NODE_NUM];	// 邻接矩阵，边的信息
	int direction;	// 是否为有向图，0无向，1有向
	int edgeNum;	// 边的个数
} MGraph;
```

**算法实现方式：**

```C
#include <stdio.h>
#include <stdlib.h>
#include "ShortPath.h"

void DijkstraMGraph(const MGraph* graph, int start, int dist[], int path[]) {
	int* mark = malloc(sizeof(int) * graph->nodeNum);
	if (mark == NULL) {
		return;
	}
	// 结构初始化(这里直接从start开始初始化了)
	for (int i = 0; i < graph->nodeNum; ++i) {
		dist[i] = graph->edges[start][i];
		mark[i] = 0;
		// 从start开始初始化dist和path
		if (graph->edges[start][i] < INF) {
			path[i] = start;
		}
		else {
			path[i] = -1;
		}
	}
	mark[start] = 1;
	path[start] = -1;
	dist[start] = 0;

	// 激活所有顶点，寻找到达每个点的最短路径
	for (int i = 0; i < graph->nodeNum - 1; ++i) {
		// 先激活权值最小的点，确定到达他的最短路径
		int mini = INF;
		int miniIndex = -1;
		for (int j = 0; j < graph->nodeNum; ++j) {
			if (mark[j] == 0 && dist[j] < mini) {
				mini = dist[j];
				miniIndex = j;
			}
		}
		// 激活并更新状态
		mark[miniIndex] = 1;
		for (int j = 0; j < graph->nodeNum; ++j) {
			if (mark[j] == 0 && dist[j] > dist[miniIndex] + graph->edges[miniIndex][j]) {
				dist[j] = dist[miniIndex] + graph->edges[miniIndex][j];
				path[j] = miniIndex;
			}
		}
	}
	free(mark);
}


void showShortPath(const int path[], int num, int end_point) {
	// 用满递增栈暂存数据，因为读取路径的时候是逆向的，而输出是正向的
	int* stack = malloc(sizeof(int) * num);
	if (stack == NULL) {
		return;
	}
	int top = -1;
	while (end_point != -1) {
		stack[++top] = end_point;
		end_point = path[end_point];
	}
	// 输出
	while (top != -1) {
		printf("\t%d", stack[top--]);
	}
	printf("\n");
	free(stack);
}

```

