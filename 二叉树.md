# 二叉树

>  所有的存储为逻辑结构服务，二叉树最好用链式存储



## 非线性结构遍历

### 深搜

​	（**一直走 往回走** 栈[系统维护/程序员维护]）

- 代码示例**（三种遍历）**

  ```C
  //typedef char element_t;
  //
  ///* 二叉树 结点结构 */
  //typedef struct node {
  //	element_t data;
  //	struct node* left;
  //	struct node* right;
  //}TreeNode;
  //
  ///* 二叉树 树头结构 */
  //typedef struct {
  //	TreeNode* root;
  //	int num;
  //}BinaryTree;
  
  // 先序遍历
  static visitPreTreeNode(TreeNode* node) {
  	if (node) {
  		visitTreeNode(node);
  		visitPreTreeNode(node->left);
  		visitPreTreeNode(node->right);
  	}
  }
  void preOrderBTree(const BinaryTree* Tree) {
  	if (Tree->root != NULL) {
  		visitPreTreeNode(Tree->root);
  	}
  }
  
  // 中序遍历
  static visitInTreeNode(TreeNode* node) {
  	if (node) {
  		visitInTreeNode(node->left);
  		visitTreeNode(node);
  		visitInTreeNode(node->right);
  	}
  }
  void inOrderBTree(const BinaryTree* Tree) {
  	if (Tree->root != NULL) {
  		visitInTreeNode(Tree->root);
  	}
  }
  
  // 后序遍历
  static visitPostTreeNode(TreeNode* node) {
  	if (node) {
  		visitPostTreeNode(node->left);
  		visitPostTreeNode(node->right);
  		visitTreeNode(node);
  	}
  }
  void postOrderBTree(const BinaryTree* Tree) {
  	if (Tree->root != NULL) {
  		visitPostTreeNode(Tree->root);
  	}
  }
  ```
  
  

### 广搜

**层次搜索（层级依次激活），发现速度  > 处理速度，缓存区（队列）**

- 不用链式，因为仅此搜索中缓存使用，不用常驻内存给其他功能，所以推荐缓存区放在栈上（顺序），注意假溢出

- 缓存里的单位元素最好采用树结点的地址（不占空间，效率高）

- 工作过程

  1. 任务初始化，先放入第一个任务
  2. 循环处理任务，直到完成所有任务

- 代码示例**（层级遍历）**

  ```
  // 广度遍历
  void levelOrderBTree(const BinaryTree* tree) {
  	TreeNode* queue[QUEUE_MAX_SIZE];
  	int front = 0, rear = 0;
  
  	// 初始化任务
  	queue[rear] = tree->root;
  	rear = (rear + 1) % QUEUE_MAX_SIZE;
  
  	// 循环执行
  	while (rear != front) {
  		//执行处理
  		visitTreeNode(queue[front]);
  		//缓存区处理
  		if (queue[front]->left != NULL) {
  			queue[rear] = queue[front]->left;
  			rear = (rear + 1) % QUEUE_MAX_SIZE;
  		}
  		if (queue[front]->right != NULL) {
  			queue[rear] = queue[front]->right;
  			rear = (rear + 1) % QUEUE_MAX_SIZE;
  		}
  		front = (front + 1) % QUEUE_MAX_SIZE;
  	}
  }
  ```
  
  



### 递归结构

**用同样的执行逻辑，把问题不断拆分的树结构。**直至问题被拆分成不可分解的最小单元后，以最快的速度返回结果。

> 更多的还是在找规律，拆到某个程度后不拆了（ 终止条件 + 拆解条件 ）
>
> 每个问题的逻辑不变，执行逻辑也不变（要学会在 实际解决过程中 切分执行片段，每个片段的执行逻辑一样）
>
> 思想：处理问题的方法一样，将大问题拆分为一个个（相同结构 状态不同）小问题

注意：实际工程中原则上不允许递归，容易引起栈溢出

【前提：任何结点的执行逻辑一样，只有”递“时候传入的状态不一样】



## 访问顺序

**每个结点都要访问一次(查看三次)**

根据处理结点的行为发生时间不同，可以分为如下三种二叉树遍历：

1. 前序遍历第一次访问到的时候
2. 中序遍历：第二次访问到的时候（第一次返回时/左子返回时）
3. 后序遍历：第三次访问到的时候（第二次返回时/右子返回时）



## 用两种遍历推出二叉树

先序确定根（前），中序确定左右子树，后序确定根（末）

1. **先序 + 中序** （根据**先序确定根**，然后根据**中序确定左右子树的先序和中序**，递归推之）
2. **后序 + 中序** （根据**后序确定根**，然后根据**中序确定左右子树的先序和中序**，递归推之）
3. **先序 + 后序** （无法确定，因为**分不出左右**（但可能会问你有几种情况））



代码示例

```C
#include <stdio.h>
#include <stdlib.h>
#include "binaryTree.h"

//typedef char element_t;
//
///* 二叉树 结点结构 */
//typedef struct node {
//	element_t data;
//	struct node* left;
//	struct node* right;
//}TreeNode;
//
///* 二叉树 树头结构 */
//typedef struct {
//	TreeNode* root;
//	int num;
//}BinaryTree;

BinaryTree* createBinaryTree(){
	BinaryTree* tree = (BinaryTree*)malloc(sizeof(BinaryTree));
	if (tree == NULL) {
		return NULL;
	}
	tree->num = 0;
	tree->root = NULL;
	return tree;
}

void initBinaryTree(BinaryTree* tree,TreeNode* root) {
	if (tree && tree->root == NULL) {
		tree->root = root;
		tree->num++;
	}
}

TreeNode* createTreeNode(const element_t val) {
	TreeNode* node = (TreeNode*)malloc(sizeof(TreeNode));
	if (node == NULL) {
		return NULL;
	}
	node->data = val;
	node->left = NULL;
	node->right = NULL;
	return node;
}

void insertBinaryTree(BinaryTree* tree, TreeNode* parent, TreeNode* left, TreeNode* right) {
	if (parent == NULL || tree == NULL) {
		return;
	}
	parent->left = left;
	parent->right = right;
	if (left)
		tree->num++;
	if (right)
		tree->num++;
}
void visitTreeNode(TreeNode* node) {
	if (node != NULL) {
		printf(" %c", node->data);
	}
}

static void releaseTreeNode(TreeNode* node) {
	if (node) {
		releaseTreeNode(node->left);
		releaseTreeNode(node->right);
		free(node);

	}
}

void releaseBinaryTree(BinaryTree* tree) {
	if (tree->root != NULL) {
		releaseTreeNode(tree->root);
	}
	tree->num = 0;
	free(tree);
}


#define QUEUE_MAX_SIZE 8
// 广度遍历
void levelOrderBTree(const BinaryTree* tree) {
	TreeNode* queue[QUEUE_MAX_SIZE];
	int front = 0, rear = 0;

	// 初始化任务
	queue[rear] = tree->root;
	rear = (rear + 1) % QUEUE_MAX_SIZE;

	// 循环执行
	while (rear != front) {
		//执行处理
		visitTreeNode(queue[front]);
		//缓存区处理
		if (queue[front]->left != NULL) {
			queue[rear] = queue[front]->left;
			rear = (rear + 1) % QUEUE_MAX_SIZE;
		}
		if (queue[front]->right != NULL) {
			queue[rear] = queue[front]->right;
			rear = (rear + 1) % QUEUE_MAX_SIZE;
		}
		front = (front + 1) % QUEUE_MAX_SIZE;
	}
}
// 深度遍历

// 先序遍历
static visitPreTreeNode(TreeNode* node) {
	if (node) {
		visitTreeNode(node);
		visitPreTreeNode(node->left);
		visitPreTreeNode(node->right);
	}
}
void preOrderBTree(const BinaryTree* Tree) {
	if (Tree->root != NULL) {
		visitPreTreeNode(Tree->root);
	}
}

// 中序遍历
static visitInTreeNode(TreeNode* node) {
	if (node) {
		visitInTreeNode(node->left);
		visitTreeNode(node);
		visitInTreeNode(node->right);
	}
}
void inOrderBTree(const BinaryTree* Tree) {
	if (Tree->root != NULL) {
		visitInTreeNode(Tree->root);
	}
}

// 后序遍历
static visitPostTreeNode(TreeNode* node) {
	if (node) {
		visitPostTreeNode(node->left);
		visitPostTreeNode(node->right);
		visitTreeNode(node);
	}
}
void postOrderBTree(const BinaryTree* Tree) {
	if (Tree->root != NULL) {
		visitPostTreeNode(Tree->root);
	}
}


```



## 二叉树的线索化

中序线索化最为常见

二叉树： 结点和结点的物理关系 -- 广深遍历

线索化： **线性关系**（中序，前序，后序 线索化）-- while遍历

线索遍历是线性遍历，更为高效，安全（不用递归），**本质是特别的链表遍历**

- 因此，线索遍历和递归遍历的代码会不太一样

**如何线索化**（以中序为例）：

1. **递归处理左子树的线索**

2. **处理当前结点的线索**

   - 当前结点的left若空，则与左**子树最后一个线索点相连**（和前驱相连）

     > **这里需要prev和cur指针**（代码示例中prev表示前驱，node表示当前结点 ）

3. **递归处理右子树的线索**

4. **处理最后一个线索点的后继**

*前序，后序只是位置换了下，如后序：2和3交换即可*

**线索遍历**（中序）：

1. **向左寻找新的初始线索点**
2. **以遍历链表的方式访问，直至线索中断**
3. **进入右子，重复1**，直到线索遍历完全（如访问到最后一个线索点的右子，NULL）

- 代码示例（中序）

  ```C
  /* 树结点结构 */
  typedef struct treenode {
  	element_t data;
  	struct treenode* left;
  	struct treenode* right;
  	int l_tag;	// 0表示子树,1表示线索
  	int r_tag;
  }TreeNode;
  
  /* 树头结构 */
  typedef struct {
  	TreeNode* root;
  	int count;
  }ThreadedBTree;
  
  static TreeNode *prev = NULL;
  static void inOrderThreadBTree(TreeNode* node) {
  	/* 终止条件 */
  	if (node == NULL) {
  		return;
  	}
  	// 左子
  	inOrderThreadBTree(node->left);
  	// 处理当前
  	if (node->left == NULL) {
  		node->left = prev;
  		node->l_tag = 1;
  	}
  	if (prev != NULL && prev->right == NULL) {
  		prev->right = node;
  		prev->r_tag = 1;
  	}
  	prev = node;
  	// 右子
  	inOrderThreadBTree(node->right);
  }
  
  /* 构建中序线索 */
  void inOrderThreading(ThreadedBTree* tree) {
  	if (tree && tree->root) {
  		prev = NULL;
  		inOrderThreadBTree(tree->root);
  		if (prev != NULL) {
  			prev->right = NULL;
  			prev->r_tag = 1;
  		}
  	}
  }
  
  /* 中序线索遍历 */
  void inOrderTravelBTree(ThreadedBTree* tree) {
  	if (tree == NULL || tree->root == NULL) {
  		return;
  	}
  	TreeNode* node = tree->root;
  	// 找初始结点
  	while (node->left && node->l_tag == 0) {
  		node = node->left;
  	}
  	// 开始遍历线索
  	while (node) {
  		// 正常访问线索
  		visitTreeNode(node);
  		if (node->r_tag == 1) {
  			node = node->right;
  		}
  		// 线索中断，要重新向后找新的初始线索结点
  		else {
  			if (node->right) {
  				node = node->right;
  			}
  			// 再次寻找新的初始结点
  			while (node->left && node->l_tag == 0) {
  				node = node->left;
  			}
  		}
  	}
  }
  ```


### 注意事项

- 线索化（递归中）记得写终止条件

- 线索遍历和递归遍历是不一样的，要理解这两种遍历的过程

- **三种线索遍历是不一样的，中序的最高效，简单**
- **线索化的时候记得处理尾结点的后继**

### 辅助理解

- 线索化的过程难理解的点：**递归，前驱指针，线索化的顺序**

- prev 用于存储cur的left，cur用于存储prev的right

- 由于left,right在非NULL状态下不能线索化，因此后面遍历的时候要重置线索起点，再遍历

- 处理完当前结点后让prev = node重置线索中的prev,从而使得每一次线索化的逻辑一致

  > 顺利完成**链表的插入**

- 用**链表**的视角来理解**线索化和线索遍历**

- **线索化**可以看成**多个链表首尾相连**，遍历线索时，**遍历到一个链表的尾结点后，转向下一个链表的首结点**（同步**链表**和**二叉线索树**）

  > 如**同步 二叉线索树的中序遍历 链表的中序遍历**

- 理解递归：相邻的两项数据，任务的分解，终止条件，相同的执行逻辑

  

  

  

  

  

  

  

  

