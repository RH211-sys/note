# 关键路径

图的存储结构：邻接表，有向无环图

重要的结构：ETV,LTV ====>EVE,LVE

先根据**邻接表**，用**拓扑算法推导出ETV和LTV**,然后再通过这两个数据**推导出EVE和LVE**，接着推导出**关键边**，推导出**关键路径**

```C
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "KeyPath.h"

// 默认要处理的图是一个源点和一个汇点的有向无环图

/* 
	用拓扑排序计算 ETV 和 LTV
	先往后计算出 ETV 后再倒推出 LTV
*/
static int topologicalOrder(const AGraph* graph, int* ETV, int* LTV) {
	// 先找到度为0的 源点
	int* inDegree = (int*)malloc(sizeof(int) * graph->nodeNum);		// 用于存储顶点入度的顺序表
	if (inDegree == NULL) {
		return -1;
	}
	memset(inDegree, 0, sizeof(int) * graph->nodeNum);


	// 存储各个顶点的入度
	for (int i = 0; i < graph->nodeNum; ++i) {
		if (graph->nodes[i].firstEdge) {
			ArcEdge* edge = graph->nodes[i].firstEdge;
			while (edge) {
				++inDegree[edge->no];
				edge = edge->next;
			}
		}
	}

	// 初始化栈，并查找出源点（最好用链式栈(头插法链表即可)，这里为了方便用顺序满递增栈)
	int* stack = (int*)malloc(sizeof(int) * graph->nodeNum);
	int top = -1;
	for (int i = 0; i < graph->nodeNum; ++i) {
		if (inDegree[i] == 0) {
			stack[++top] = i;
		}
	}

	// 开始统计ETV(顶点最早时间)，并计数(每出一个记一次)，同时寻找汇点
	int* topOut = (int*)malloc(sizeof(int) * graph->nodeNum);	// 这里以存储顶点的方式寻找汇点，汇点在最后一个,为了统一，同样是满递增栈
	int index = -1;
	int count = 0;
	while (top > -1) {
		// 出栈的处理
		int nodeTmp = stack[top--];
		topOut[++index] = nodeTmp;
		++count;
		if (graph->nodes[nodeTmp].firstEdge) {
			ArcEdge* edge = graph->nodes[nodeTmp].firstEdge;
			while (edge) {
				// 每遍历一条边，对应顶点的入度-1
				--inDegree[edge->no];
				if (inDegree[edge->no] == 0) {
					stack[++top] = edge->no;
				}
				// 顺推：如果现在到此点的 最早时间 大于这个点原来的 最早时间，那么要更新，因为以大的时间为主，这样才能保证前面全部完成
				if (ETV[edge->no] < ETV[nodeTmp] + edge->weight) {
					ETV[edge->no] = ETV[nodeTmp] + edge->weight;
				}
				edge = edge->next;
			}
		}
	}
	// 判断是否有环
	if (count < graph->nodeNum) {
		free(inDegree);
		free(stack);
		free(topOut);
		return -1;
	}
	free(inDegree);
	free(stack);
	// 若无环，开始倒推出LTV，从事先记录的汇点开始
	// 先初始化每个最晚时间为无穷大，这里一定是汇点的时间最大
	int tmp = topOut[index];
	for (int i = 1; i < graph->nodeNum; ++i) {
		LTV[i] = ETV[tmp];
	}
	while (index > 0) {
		// 出栈处理
		int nodeTmp = topOut[index--];
		if (graph->nodes[nodeTmp].firstEdge) {
			ArcEdge* edge = graph->nodes[nodeTmp].firstEdge;
			while (edge) {
				// 倒推：如果预计此点的 最晚时间 小于这个点原来的 最晚时间，那么要更新，因为以小的时间为主，否则无法完成后续
				if (LTV[nodeTmp] > LTV[edge->no] - edge->weight) {
					LTV[nodeTmp] = LTV[edge->no] - edge->weight;
				}
				edge = edge->next;
			}
		}
	}
	free(topOut);
}

// 输出malloc的指针中的每一项
static void showTable(const int* table, int n, const char* name) {
	printf("%s\n", name);
	for (int i = 0; i < n; ++i) {
		printf("\t%d", table[i]);
	}
	printf("\n");
}



/*
	目的：给一个邻接表的图结构，计算出这个图的关键路径
	如何计算：用拓扑排序先计算ETV和LTV，同时判断是否有环，再推导关键边和关键路径
*/
void keyPath(const AGraph* graph) {
	// 引入ETV和LTV并初始化，用于存储每个顶点的最早和最晚时间
	int* ETV = malloc(sizeof(int) * graph->nodeNum);
	int* LTV = malloc(sizeof(int) * graph->nodeNum);
	memset(ETV, 0, sizeof(int) * graph->nodeNum);
	memset(LTV, 0, sizeof(int) * graph->nodeNum);
	
	// 用拓扑排序计算ELV和LTV
	topologicalOrder(graph, ETV, LTV);
	showTable(ETV, graph->nodeNum, "ETV: ");
	showTable(LTV, graph->nodeNum, "LTV: ");
	
	/* 
		推导关键路径( 若 ETE == LTE 则为关键边 )
		ETE:最早的边，和弧尾的ETV一样
		LTV:最晚的边，计算：是 弧头的LTV-边的权重
	*/
	for (int i = 0; i < graph->nodeNum; ++i) {
		ArcEdge* edge = graph->nodes[i].firstEdge;
		while (edge) {
			if (ETV[i] == LTV[edge->no] - edge->weight) {
				printf("<%s> --- %d --- <%s>\n", graph->nodes[i].show, edge->weight, graph->nodes[edge->no].show);
			}
			edge = edge->next;
		}
	}
	free(ETV);
	free(LTV);
}

```

