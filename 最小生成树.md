# 最小生成树

在一个图中，寻找权值和最小的连通图
注意要确保无环

## **Kruskal**

**需要的结构**

- 边集数组

  > 这可以尝试从 邻接矩阵 转换过来
  
  | 边下标（第几条边） | begin(头) | end(尾) | weight(权值) |
  | :----------------: | :-------: | :-----: | :----------: |
  |         0          |           |         |              |
  |         1          |           |         |              |
  |         2          |           |         |              |
  
  

**结构定义**

```C
/* Kruskal算法：
 *	m条边，n个顶点，m条边中找n-1条边，n-1条边之和是最小的，且是生成树
 *	约束，必须保证这条边加入后，不会产生环
 * 如何判断一条边加入后，无向图是否含有环，引入并查集
 * 算法思路：
 *	1. 构建一个边集数组，进行升序排列，从小到大一一获取，满足条件：
 *	2. 加入的这条边是否会形成环路，形成这条边放弃，找下一条边
 	   否则加进去，并记录
 */
 // 使用邻接矩阵来表示无向图

typedef struct {
	int		begin;				// 边的起点（顶点1）
	int		end;				// 边的终点（顶点2）
	int		weight;				// 边的权值
} EdgeSet;

// 从邻接矩阵中初始化边集数组
int initEdgeSet(const MGraph* graph, EdgeSet* edges);
// 排序边集数组
void sortEdgeSet(EdgeSet* edges, int num);
// Kruskal生成最小生成树，返回权值和
int KruskalMGraph(const MGraph* graph, const EdgeSet* edges, int num, EdgeSet* result);
```

**用并查集生成最小生成树**

```C
static int getRoot(const int* uSet, int a) {
	while (a != uSet[a]) {
		a = uSet[a];
	}
	return a;
}

int KruskalMGraph(const MGraph* graph, const EdgeSet* edges, int num, EdgeSet* result) {
	int weigthSum = 0;
	// 1.初始化并查集(顺序结构)，用于检测是否构成环
	int* uSet = (int*)malloc(sizeof(int) * graph->nodeNum);
	if (uSet == NULL) {
		return 0;
	}
	for (int i = 0; i < graph->nodeNum; ++i) {
		uSet[i] = i;
	}
	int count = 0;

	// 从最小的开始向上找，用于检测这两个结点是否在同一个集团
	for (int i = 0; i < num; ++i) {
		int rootA = getRoot(uSet, edges[i].begin);
		int rootB = getRoot(uSet, edges[i].end);
		// 检查是否构成环
		if (rootA != rootB) {
			// 合并新找到的边
			result[count].begin = edges[i].begin;
			result[count].end = edges[i].end;
			result[count].weight = edges[i].weight;
			weigthSum += result[count].weight;
			count++;

			uSet[rootA] = rootB;
			// 检查此次合并后时候生成完成
			if (count == graph->nodeNum - 1) {
				break;
			}
		}
	}
	free(uSet);
	return weigthSum;
}
```

## **Prim**

以顶点为基准，实质上每次激活的还是一条边

步骤：

1. 根据传入的startV，激活当前节点，更新相关的辅助空间

2. 动态激活节点，直到全部激活为止

3. 每激活一个顶点后，更新cost数组，以k为行下标，或者他到其他节点的权值，更新cost数组

   |      ID(下标代表数据)       |  0   |  1   |  2   | ···  |
   | :-------------------------: | :--: | :--: | :--: | :--: |
   |  cost(边的权值，默认是INF)  |      |      |      |      |
   |       mark(激活状态)        |      |      |      |      |
   | visit(哪个顶点访问当前顶点) |      |      |      |      |

   > 这样能保证每次激活的点对应的权值最小

   ```C
   #include <stdio.h>
   #include <stdlib.h>
   #include "Prim.h"
   
   /* cost: 图中到各顶点边的权值
    * mark: 图中顶点的激活状态
    * visit:哪个顶点访问当前顶点
    * startV: 第一个激活的顶点编号
    */
   
   // 返回最小权值和
   int PrimMGraph(const MGraph* graph, int startV, EdgeSet* result) {
   	int sum = 0;
   	int* cost = malloc(sizeof(int) * graph->nodeNum);
   	int* mark = malloc(sizeof(int) * graph->nodeNum);
   	int* visit = malloc(sizeof(int) * graph->nodeNum);
   	if (cost == NULL || mark == NULL || visit == NULL) {
   		return 0;
   	}
   
   	// 初始化一个激活结点和其他结点
   	for (int i = 0; i < graph->nodeNum; ++i) {
   		cost[i] = graph->edges[startV][i];
   		mark[i] = 0;
   		if (cost[i] < INF) {
   			visit[i] = startV;
   		}
   		else {
   			visit[i] = -1;
   		}
   	}
   	mark[startV] = 1;
   
   	// 动态激活所有结点
   	for (int i = 0; i < graph->nodeNum -1; ++i) {
   		// 先激活权值数组中未激活结中最小的结点
   		int miniCost = INF;
   		int nodeID = 0;
   		for (int j = 0; j < graph->nodeNum; ++j) {
   			if (mark[j] == 0 && cost[j] < miniCost) {
   				nodeID = j;
   				miniCost = cost[j];
   			}
   		}
   		// 激活并记录
   		result[i].begin = nodeID;	
   		result[i].end = visit[nodeID];	
   		result[i].weight = miniCost;
   		mark[nodeID] = 1;
   		sum += miniCost;
   
   		// 以激活结点为基准，更新数组
   		for (int j = 0; j < graph->nodeNum; ++j) {
   			if (cost[j] > graph->edges[nodeID][j]) {
   				cost[j] = graph->edges[nodeID][j];
   				visit[j] = nodeID;
   			}
   		}
   
   	}
       
   	free(cost);
   	free(mark);
   	free(visit);
   	return sum;
   }
   ```

   
