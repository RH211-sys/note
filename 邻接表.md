# 邻接表

结构定义：
**顶点集**+顶点集 中**每一个顶点的邻接边**

```C
// 边的结构
typedef struct arcEdge {
	int		no;				// 边空间的索引号，以顶点到no的关系 出度关系
	int		weight;			// 这条边的权值
	struct arcEdge* next;	// 顶点的下一个出度
} ArcEdge;

// 顶点结构
typedef struct {
	int		no;				// 顶点的编号
	char* show;				// 顶点的显示内容
	ArcEdge* firstEdge;		// 指向第一个出度的节点，后续出度由边的next来指定
} ArcNode;

// 邻接表的图结构（图头）
typedef struct {
	ArcNode* nodes;			// 图中顶点的集合，邻接 连续空间(邻接表的顶点集)
	int	nodeNum;			// 图中顶点的个数
	int	directed;			// 是否有向图
	int edgeNum;			// 图中的边数
	int* visited;			// 以访问编号，遍历的辅助数组
} AGraph;
```

样例：

```

顶点A(0): → B(1,权重2) → C(2,权重3) → NULL
顶点B(1): → A(0,权重2) → C(2,权重4) → D(3,权重1) → NULL
顶点C(2): → A(0,权重3) → B(1,权重4) → D(3,权重5) → NULL
顶点D(3): → B(1,权重1) → C(2,权重5) → E(4,权重6) → NULL
顶点E(4): → D(3,权重6) → NULL
```

**用顺序存储存放图的结点 / 邻接表的顶点集**（C语言理解加深：指针/数组 & 顺序存储）

**邻接表插入**
链表头插

```C
void addAGraph(AGraph* graph, int x, int y, int w) {
	if (x<0 || x>=graph->nodeNum || y<0 || y>=graph->nodeNum || x == y) {
		return;
	}
	// 在邻接表的该层用头插法
	ArcEdge* edge = createArcEdge(y, w);
	edge->next = graph->nodes[x].firstEdge;
	graph->nodes[x].firstEdge = edge;
	graph->edgeNum++;
	// 无向图还要在另一侧头插
	if (graph->directed == 0) {
		edge = createArcEdge(x, w);
		edge->next = graph->nodes[y].firstEdge;
		graph->nodes[y].firstEdge = edge;
		graph->edgeNum++;
	}
}
```

**邻接表的DFS遍历**
核心思想不变，仍是深入和回溯

```C
void DFSAGraphTravel(const AGraph* graph, int v) {
	// 归(这里和while重复且如果下面的访问放到上面会有点乱，故省略)
	//if (graph->nodes[v].firstEdge == NULL) {
	//	return;
	//}
	// 递
	visitAGraphNode(&graph->nodes[v]);
	graph->visited[v] = 1;
	ArcEdge* p = graph->nodes[v].firstEdge;
	while (p) {
		if (graph->visited[p->no] == 0) {
			DFSAGraphTravel(graph, p->no);
		}
		p = p->next;
	}
}
```

**BFS**
和邻接矩阵不同，这里逻辑如下：
初始化缓存区，先处理缓存区的一项数据，再将这项数据对应的层入队

> 是入队后视为已被遍历，而不是在拿出来处理后视为被遍历

```C
void BFSAGraphTravel(const AGraph* graph, int v) {
	int* que = (int*)malloc(sizeof(int) * graph->nodeNum);
	if (que == NULL) {
		return;
	}
	int front = 0, rear = 0;

	rear = (rear + 1) % graph->nodeNum;
	que[rear] = v;
	graph->visited[v] = 1;

	while (front != rear) {
		// 处理一个缓存数据
		front = (front + 1) % graph->nodeNum;
		visitAGraphNode(&graph->nodes[que[front]]);

		// 存放该缓存对应层的数据
		ArcEdge* edge = graph->nodes[que[front]].firstEdge;
		while (edge) {
			if (graph->visited[edge->no] == 0) {
				rear = (rear + 1) % graph->nodeNum;
				que[rear] = edge->no;
				graph->visited[edge->no] = 1;
			}
			edge = edge->next;
		}
	}
	free(que);
}
```

