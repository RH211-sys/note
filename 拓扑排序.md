# 拓扑排序

**目的**：检测一系列的**活动/事务/任务**流程中是否出现循环结构

> 因此这个算法只能判断有向图是否带环

**结构**

```
由于这个算法只能判断有向图是否带环
因此只能处理有向图
故这里用邻接表存储图结构
```

**思路**

```
1. 找到图中，入度为0的顶点，把这些顶点放入缓存中（栈、队列）
2. 从缓存区中，取出一个顶点，放入到结果集
3. 这个顶点的事件发生了，顶点的对应出度消失，有其他顶点的入度随之消失。
4. 又会增加新的入度为0的顶点出现，如果出现，放入缓存
5. 重复2-3-4
6. 取出缓存中的所有顶点数量：a1，图中顶点的总个数为a2;
	a1 == a2则没有环，a1 < a2则有环
```

**邻接表定义**

```C
// 边的结构
typedef struct arcEdge {
	int		no;				// 边空间的索引号，以顶点到no的关系 出度关系
	int		weight;			// 这条边的权值
	struct arcEdge* next;	// 顶点的下一个出度
} ArcEdge;

// 顶点结构
typedef struct {
	int		no;				// 顶点的编号
	char* show;				// 顶点的显示内容
	ArcEdge* firstEdge;		// 指向第一个出度的节点，后续出度由边的next来指定
} ArcNode;

// 邻接表的图结构（图头）
typedef struct {
	ArcNode* nodes;			// 图中顶点的集合，邻接 连续空间(邻接表的顶点集)
	int	nodeNum;			// 图中顶点的个数
	int	directed;			// 是否有向图
	int edgeNum;			// 图中的边数
	int* visited;			// 以访问编号，遍历的辅助数组
} AGraph;

AGraph* createAGraph(int n);			// 产生n个节点的邻接表的图
void releaseAGraph(AGraph* graph);		// 释放图

void initAGraph(AGraph* graph, char* names[], int num, int directed);
void addAGraph(AGraph* graph, int x, int y, int w);

// 深度遍历
void DFSAGraphTravel(const AGraph* graph, int v);
// 广度遍历
void BFSAGraphTravel(const AGraph* graph, int v);

// 重置访问标记
void resetAGraphVisited(AGraph* graph);
void visitAGraphNode(const ArcNode* node);
```

**算法实现**

- 这里用链栈来作为缓存，这样只用维护一个top结点

```C
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "topologicalSort.h"

int TopologicalSortAGraph(const AGraph* graph) {
	// 入度统计
	int* inDegrees = malloc(sizeof(int) * graph->nodeNum);
	if (inDegrees == NULL) {
		return -1;
	}
	memset(inDegrees, 0, sizeof(int) * graph->nodeNum);

	// 初始化入度
	for (int i = 0; i < graph->nodeNum; ++i) {
		ArcEdge* edge = graph->nodes[i].firstEdge;
		while (edge) {
			 ++inDegrees[edge->no];
			edge = edge->next;
		}
	}
	// 初始化栈
	typedef struct node{
		int no;
		struct node* next;
	} Top;
	Top* top = NULL;
	for (int i = 0; i < graph->nodeNum; ++i) {
		if (inDegrees[i] == 0) {
			Top* temp = malloc(sizeof(Top));
			if (temp == NULL) {
				return -1;
			}
			temp->next = top;
			temp->no = i;
			top = temp;
		}
	}

	// 开始
	int count = 0;
	while (top) {
		// 处理栈中的事务，并更新状态
		visitAGraphNode(&graph->nodes[top->no]);
		count++;
		// 更新状态
		ArcEdge* edge = graph->nodes[top->no].firstEdge;
		// 删除处理好的结点
		Top* temp = top;
		top = top->next;
		free(temp);
		// 更新inDegrees并将入度为0的顶点入栈
		while (edge) {
			inDegrees[edge->no]--;
			if (inDegrees[edge->no] == 0) {
				temp = malloc(sizeof(Top));
				temp->next = top;
				temp->no = edge->no;
				top = temp;
			}
			edge = edge->next;
		}
	}

	free(inDegrees);

	if (count == graph->nodeNum) {
		return 1;
	}
	else {
		return 0;
	}
}

```

