# 哈夫曼树和哈夫曼编码

## 哈夫曼树

若有**n个待编码结点**，则这棵树**需要2n-1个结点存储**
因为**不仅要存储结点，还要存储“权值父节点”**

结构定义（这里用顺序空间）

```C
/* 
   Huffman树通过待编码的节点数量，计算出总共的节点个数 m = 2*n -1个
   用数组0的单元表示无效节点，从1号单元开始进行填充，那么申请2*n个空间
 */
typedef struct {
	int weight;			// 节点的权值
	int parent;			// 该节点的父节点编号，0值表示该节点就是根
	int lChild, rChild;	// 指向该节点的左右孩子节点的编号
} HuffmanNode, * HuffmanTree;
```

每个结点都有**父编号**，**左右子编号**，**权值**

哈夫曼树的构建
方法：每次从这组还没有父结点的数据中找两个权值最小的，组合，弄出一个父节点

```C
static void selectTwoMin(HuffmanTree tree, int n, int* s1, int* s2) {
	*s1 = *s2 = 0;
	for (int i = 1; i <= n; ++i) {
		if (tree[i].parent == 0) {
			if (*s1 == 0) {
				*s1 = i;
			}
			else if (*s2 == 0) {
				*s2 = i;
				if (tree[*s1].weight > tree[*s2].weight) {
					int t = *s1;
					*s1 = *s2;
					*s2 = t;
				}
			}
			else {
				// 比较权值大小，更新最小的2个节点下标
				if (tree[i].weight < tree[*s1].weight) {
					*s2 = *s1;
					*s1 = i;
				}
				else if (tree[i].weight < tree[*s2].weight) {
					*s2 = i;
				}
			}
		}
	}
}

HuffmanTree createHuffmanTree(const int* w, int n) {
	// 实际结点个数
	int allNode = 2 * n - 1;
	// 顺序空间
	HuffmanTree tree = malloc(sizeof(HuffmanNode) * (allNode + 1));
	if (tree == NULL) {
		return NULL;
	}

	// 初始化各个结点
	for (int i = 1; i <= allNode; ++i) {
		tree[i].parent = tree[i].rChild = tree[i].rChild = 0;
		tree[i].weight = 0;
	}
	// 输入权值
	for (int i = 1; i <= allNode; ++i) {
		tree[i].weight = w[i - 1];
	}
	// 开始构建Huffman
	/* 找两个最小的，组合，并弄出一个父节点 */
	int index1, index2;	// 两个权值最小的结点索引
	for (int i = n + 1; i <= allNode; ++i) {	// 参见那张例表
		selectTwoMin(tree, i-1, &index1, &index2);
		// 结点组合，生成父节点的信息
		tree[index1].parent = tree[index2].parent = i;
		tree[i].lChild = index1;
		tree[i].rChild = index2;
		tree[i].weight = tree[index1].weight + tree[index2].weight;
	}
	return tree;
}
```

## 哈夫曼编码

- 基于哈夫曼树，通过**左0右1**的方式编码（这里的**左0右1**只是举例，也可以左1右0等方式）
  这样可以**通过字符使用的频率进行编码**，使用频率高的字符所占用的内存小(如A编码：10)，使用频率低的占用内存高(如TAB键编码:111110)

- 读取：如上述左0右1的例子，先一直存，存到0的时候将存的内容和编码表配对，随后读取

- 编码方法：用一个满递减栈存储编码信息（因为要从待编码结点开始向上走到根），每走完一次为相应的结点编码

  ```C
  /* Huffman编码，用一个字符数组空间来保存每个符号的编码字符串
   * char *codes[n];		HuffmanCode codes[n];
   */
  
  // 从n个叶子节点找到根节点，逆向求每个叶子的对应的编码
  HuffmanCode* createHuffmanCodes(HuffmanTree tree, int n) {
  	// 构建和初始化
  	HuffmanCode* codes = (HuffmanCode*)malloc(sizeof(HuffmanCode) * n);
  	if (codes == NULL) {
  		return NULL;
  	}
  	memset(codes, 0, sizeof(codes));
  
  	// 编码 临时栈，逆序存储
  	char* temp = malloc(sizeof(char) * (n + 1));
  
  	for (int i = 1; i <= n; ++i) {
  		int start = n - 1;
  		temp[start] = '\0';
  		int cur = i;
  		int parent = tree[i].parent;
  		// 编码
  		while (parent) {
  			--start;
  			temp[start] = (tree[parent].lChild == cur) ? '0' : '1';
  			cur = parent;
  			parent = tree[parent].parent;
  		}
  		// 存储该字符的编码结果
  		codes[i - 1] = malloc(sizeof(char) * (n - start));// 编码字符个数
  		strcpy(codes[i - 1], &temp[start]);
  	}
  	free(temp);
  	return codes;
  }
  ```

  

