# 并查集

目的：分组，便于管理

## 快速查找并查集(QuickFindSet)

查找速度：O(1)
合并速度：O(n)

结构定义

```C
typedef int element_t;

typedef struct {
	element_t* data;		// 存放数据的，采用顺序存储，自带了下标索引，作为关系数组里的索引值
	int* groupID;			// groupID[i] 表示第i个元素说归属的集团编号
	int n;					// 集合中元素的个数
	int capacity;			// 集合的容量
} QuickFindSet;
```



- **判断某两个元素是否在同一个集团**
  方法：直接访问集合groupID对应的下标的值

  ```C
  // 检查某两个元素是否在同一个集团中
  int isSameQF(const QuickFindSet* setQF, element_t a, element_t b) {
  	// 先找这两个元素所在的下标
  	int indexA = -1; int indexB = -1;
  	indexA = findIndex(setQF,a);
  	indexA = findIndex(setQF,b);
  	if (indexA == -1 || indexB == -1) {
  		return 0;
  	}
  	return (setQF->groupID[indexA] == setQF->groupID[indexB]) ? 1 : 0;
  }
  
  ```



- **合并 a和b这两个元素所在的集合**
  方法：遍历，把其中一个集团的所有元素成员的集团编号改为另一个集团

  ```C
  // 遍历查找该元素的索引
  static int findIndex(const QuickFindSet* setQF, element_t e) {
  	for (int i = 0; i < setQF->n; ++i) {
  		if (setQF->data[i] == e) {
  			return i;
  		}
  	}
  	return -1;
  }
  
  void unionQF(const QuickFindSet* setQF, element_t a, element_t b) {
  	// 先遍历找到对应的索引
  	int indexA = -1; int indexB = -1;
  	indexA = findIndex(setQF, a);
  	indexB = findIndex(setQF, b);
  	
  	// 检查这两个元素是否在同一个集团
  	int groupA = setQF->groupID[a];
  	int groupB = setQF->groupID[b];
  	if (groupA == groupB) {
  		return;
  	}
  	// 将groupB的所有成员改为groupA
  	for (int i = 0; i < setQF->n; ++i) {
  		if (setQF->groupID[i] == groupB) {
  			setQF->groupID[i] == groupA;
  		}
  	}
  }
  ```

  

## 快速合并并查集(QuickUnionSet)

查找速度：O(logn)
合并速度：O(logn)

核心：每个元素只保存父编号，通过不断找父元素，最终找到根元素编号

- **存储：**
  parentID通过“静态树”的结构存储（存储每个元素的父下标） 
  当这个元素的父下标为自己时，该元素作为这个集团的祖先
  size数组表示当i为根元素时，表示这棵树的节点个数

  ```C
  typedef int element_t;
  typedef struct {
  	element_t* data;
  	int* parentID;			// parentID[i]表示第i个元素的父元素编号
  	int* size;				// size[i]表示：当i为根元素时，表示这棵树的节点个数
  	int n;					// 集合中有效元素的个数
  	int capacity;			// 集合的总容量
  } QuickUnionSet;
  ```

- **判断两个元素是否同一个集团**
  方法：判断它们的根是否相同

  ```C
  // 路径压缩
  static int findRootIndexWithPathCompress(const QuickUnionSet* setQU, element_t e) {
  	int index = -1;
  	index = findIndex(setQU, e);
  	if (index == -1) {
  		return -1;
  	}
  	// 创建链式栈（链表头插）,在往根查找的过程中记录所经过的下标，方便后续的路径压缩
  	SetNode* stackTop = NULL;
  	while (setQU->parentID[index] != index) {
  		// 入栈，记录路径
  		SetNode* newNode = (SetNode*)malloc(sizeof(SetNode));
  		newNode->index = index;
  		newNode->next = stackTop;
  		stackTop = newNode;
  		index = setQU->parentID[index];
  	}
  	// 根节点找到了(index是根)，路径压缩，上一步找根的路径的节点都更新其parentID指向根索引（index）
  	while (stackTop != NULL) {
  		SetNode* temp = stackTop;
  		setQU->parentID[stackTop->index] = index;
  		stackTop = stackTop->next;
  		free(temp);
  	}
  	return index;
  }
  
  int isSameQU(const QuickUnionSet* setQU, element_t a, element_t b) {
  	int rootA = findRootIndexWithPathCompress(setQU, a);
  	int rootB = findRootIndexWithPathCompress(setQU, b);
  	if (rootA == -1 || rootB == -1) {
  		return 0;
  	}
  	return rootA == rootB;
  }
  ```

- 合并
  方法：将高度低的集合合并到高度高的

  ```C
  void unionQU(const QuickUnionSet* setQU, element_t a, element_t b) {
  	int rootA = findRootIndexWithPathCompress(setQU, a);
  	int rootB = findRootIndexWithPathCompress(setQU, b);
  	if (rootA == -1 || rootB == -1) {
  		return 0;
  	}
  	if (rootA == rootB) {		// 已经在同一个集合里
  		return;
  	}
  	// 高度低的合并到高度高的集合
  	if (setQU->size[rootA] < setQU->size[rootB]) {
  		setQU->parentID[rootA] = rootB;
          // 这里合并后要修改 根所对应的size
  		setQU->size[rootB] += setQU->size[rootA];
  	}
  	else {
  		setQU->parentID[rootB] = rootA;
  		setQU->size[rootA] += setQU->size[rootB];
  	}
  }
  ```

  

