# 栈

## 思想

- 先进后出，**倒退**概念(**倒着走**)
- **回溯/递归**（本质还是**倒着走**），暂存

## 应用示例

- 链表倒序，历史记录，迷宫系统

***

## 顺序栈

顺序栈中，top指向位置和操作行为上，能够分成**四种插入行为**：

1. 满栈：永远指向当前有效位置
2. 空栈：永远指向最后一个有效位置的下一个 **空** 位置
3. 递增栈：入栈都是++（top在低位）
4. 递减栈：入栈都是 --（top在高位）

> - 以及组合的方式：满递增，满递减，空递增，空递减，**要注意不同栈类型的匹配**
> - C语言 编译器维护一个**满递减栈**
> - 还有栈的 **上溢** 和 **下溢**

为什么不扩容

- 因为顺序栈中有 **溢出** 概念，加扩容不符合逻辑结构的定义

  > 物理结构的建造是为了完整地实现逻辑结构
  >
  > 要根据目标，需求来写实现

代码示例：

```C
#include <stdio.h>
#include "ArrayStack.h"

ArrayStack.h
#define MAX_CAPACITY 100
typedef int element_t;
/* 顺序栈 连续空间 结构定义 */
typedef struct {
	element_t data[MAX_CAPACITY];
    int top;
}ArrayStack;

/* 初始化 */
void initArrayStack(ArrayStack* stack) {
	stack->top = -1;
}

/* 入栈 */
void pushArrayStack(ArrayStack* stack, element_t val) {
	if (stack->top >= MAX_CAPACITY - 1) {
		printf("stack full!\n");
		return;
	}
	++stack->top;
	stack->data[stack->top] = val;
}

/* 出栈 */
void popArrayStack(ArrayStack* stack) {
	if (!isEmptyArrayStack(stack)) {
		printf("stack none!\n");
		return;
	}
	--stack->top;
}

/* 得到栈顶的值 */
element_t getArrayStackTopVal(ArrayStack* stack) {
	if (isEmptyArrayStack(stack)) {
		return stack->data[stack->top];
	}
	printf("stack null!\n");
	return -1;
}

/* 判断栈是否为空 */
int isEmptyArrayStack(ArrayStack* stack) {
	if (stack->top == -1) {
		return 0;
	}
	return 1;
}

/* 清空 */
void clearArrayStack(ArrayStack* stack){
	stack->top = -1;
}
```



## 链式栈

**只能头插头删**(分别是**入栈和出栈**)

- 因为出栈的时候**要考虑指针回退**

代码示例：

```C
#include <stdio.h>
#include <stdlib.h>
#include "LinkStack.h"

typedef int value_t;
/* 链式栈 结点结构定义 */
typedef struct node{
	value_t data;
	struct node* next;
}SNode;

/* 链式表 栈表头结构定义 */
typedef struct {
	SNode* top;
	int num;
}LinkStack;

/* 初始化 */
void initLinkStack(LinkStack* stack) {
	stack->top = NULL;
	stack->num = 0;
}

/* 入栈 */
void pushLinkStack(LinkStack* stack, value_t val) {
	SNode* node = (SNode*)malloc(sizeof(SNode));
	if (node == NULL) {
		printf("无法分配新节点的内存！\n");
		return;
	}
	//头插入栈
	node->data = val;
	node->next = stack->top;
	stack->top = node;
	++stack->num;
}

/* 出栈 */
void popLinkStack(LinkStack* stack) {
	if (!isEmptyLinkStack(stack)) {
		printf("stack is null!\n");
		return;
	}
	SNode* temp = stack->top;
	stack->top = stack->top->next;
	--stack->num;
	free(temp);
}

/* 查看栈顶元素 */
value_t getLinkStackVal(LinkStack* stack) {
	if (!isEmptyLinkStack(stack)) {
		printf("stack is null!\n");
		return -1;
	}
	return stack->top->data;
}

/* 判断是否为空 */
int isEmptyLinkStack(LinkStack* stack) {
	if (stack->top == NULL && stack->num == 0) {
		return 0;
	}
	return 1;
}

/* 清空 */
void clearLinkStack(LinkStack* stack){
	SNode* node = stack->top;
	SNode* temp;
	while (node) {
		temp = node->next;
		free(node);
		node = temp;
	}
}
```



## 补充

1. 指针思想：不一定是地址，本质上是指向目标的对象

   >  如顺序栈中的栈顶指针

2. 连续空间（顺序结构）都能用下标索引

   > 指针也能用，但下标更好更方便更安全
   >
   > 但链式结构就只能用指针了

3. 写接口可能要考虑特殊形式

4. 代码中记得写写注释

   > 增加维护性，易读性，同时能理清思路，也便于修改和*寻找相关代码（较少）*

- 顺序：连续空间，索引
- 链式：结点，指针，链头
  - 思想：备份，先引入后删除...（可参考 **链表** 这个经典链式结构）

